export default function Page() {
  return (
    <main style={{ fontFamily: 'Arial, sans-serif', padding: 32 }}>
      <h1>Checkout Workflow MCP</h1>
      <p>MCP server is running.</p>
      <p>Use the <code>/sse</code> endpoint inside ChatGPT Custom MCP.</p>
    </main>
  );
}
